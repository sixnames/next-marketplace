name: Build and Test

on:
  pull_request:
    branches: [ develop ]

jobs:
#  apiTest:
#    runs-on: ubuntu-latest
#    strategy:
#      matrix:
#        node-version: [12.x]
#
#    steps:
#      - uses: actions/checkout@v2
#      - name: Development
#        uses: actions/setup-node@v1
#        with:
#          node-version: ${{ matrix.node-version }}
#
#      - run: yarn install:api
#      - run: yarn test:api

#  clientTest:
  tesiting:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - uses: actions/checkout@v2
      - name: Development
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - run: docker-compose -f docker-compose.testing.yml up -d
      - run: yarn install:api
      - run: yarn test:api
      - run: yarn build:api:testing
      - run: yarn start:api:testing
      - run: yarn install:client
      - run: yarn build:client:testing
      - run: yarn start:client:testing
      - run: yarn test:client
