name: Build, Test, Deploy to the develop

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
#  apiTest:
#    runs-on: ubuntu-latest
#    strategy:
#      matrix:
#        node-version: [12.x]
#
#    steps:
#      - uses: actions/checkout@v2
#      - name: Development
#        uses: actions/setup-node@v1
#        with:
#          node-version: ${{ matrix.node-version }}
#
#      - run: yarn install:api
#      - run: yarn test:api
#
#  appTest:
#    runs-on: ubuntu-latest
#    strategy:
#      matrix:
#        node-version: [12.x]
#
#    steps:
#    - uses: actions/checkout@v2
#    - name: Development
#      uses: actions/setup-node@v1
#      with:
#        node-version: ${{ matrix.node-version }}
#
#    - run: yarn
#    - run: yarn install:app
#    - run: yarn up:testing
#    - run: yarn test:app

  deploy:
#    needs: [apiTest, appTest]
    runs-on: ubuntu-latest
    steps:
      - name: ls -a via ssh
        uses: garygrossgarten/github-action-ssh@release
        with:
          command: cd site && git pull && docker-compose -f docker-compose.stage.yml up --build -d
          host: ${{ secrets.DEV_HOST }}
          username: ${{ secrets.DEV_USERNAME }}
          privateKey: ${{ secrets.DEV_PRIVATE_KEY}}

